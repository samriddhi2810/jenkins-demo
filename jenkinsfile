pipeline {
  agent any

  stages {
    stage('Test') {
      steps {
        echo 'Hello from Jenkins 🚀'
      }
    }

    stage('Docker Test') {
      steps {
        sh 'docker --version'
      }
    }
  }
}